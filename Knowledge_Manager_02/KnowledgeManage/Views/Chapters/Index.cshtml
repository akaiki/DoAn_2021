@model IEnumerable<KnowledgeManage.Models.Chapter>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name_Chap)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Key_Chap)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Link_Chap)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name_Chap)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Key_Chap)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Link_Chap)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Chap">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Chap">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Chap">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
