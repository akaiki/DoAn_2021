@model IEnumerable<KnowledgeManage.Models.Relationship>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Knowledge_A)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Knowledge_B)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Knowledge_A.Id_Knowledge)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Knowledge_B.Id_Knowledge)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Relationship">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Relationship">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Relationship">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
