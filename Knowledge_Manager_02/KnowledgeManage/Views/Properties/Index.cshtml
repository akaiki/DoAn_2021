@model IEnumerable<KnowledgeManage.Models.Property>

@{
    ViewData["Title"] = "Index";
}

<div class="container bg-light"> 
    <h1>Index</h1>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Link_Property)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name_Property)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Lesson)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Link_Property)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name_Property)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Lesson.Id_Lesson)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id_Property">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.Id_Property">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id_Property">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>